// Stub de teste para o pacote `server-only`. Em produção ele garante, via
// resolução do bundler, que um módulo nunca vá para o cliente. Sob o Vitest
// (Node) não há bundler de cliente, então é um no-op seguro.
export {};
